<div class="flex flex-col text-gray-700 text-center mt-2">
  <% if controller_name != 'passwords' and controller_name != 'registrations' %>
    <%= link_to 'Forgot your password?', new_password_path(resource_name), class: 'underline' %>
  <% end %>
</div>
