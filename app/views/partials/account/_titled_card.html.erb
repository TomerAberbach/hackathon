<h1 class="text-3xl mb-3 <%= local_assigns.fetch(:title_class, '') %>"><%= yield(:title) %></h1>

<div class="md:text-xl max-w-5/6 p-8 sm:p-10 rounded shadow <%= local_assigns.fetch(:class, '') %>">
  <%= yield %>
</div>
